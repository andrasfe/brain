# Copy to deploy/joplin.env and edit. Used by joplin-server.compose.yml.
POSTGRES_USER=joplin
POSTGRES_PASSWORD=change-this-strong-secret
POSTGRES_DB=joplin
# Public URL of the server (change if exposing beyond localhost):
APP_BASE_URL=http://localhost:22300

httpx>=0.27
python-dotenv>=1.0
PyYAML>=6.0
# Optional — eyes + hands on the host computer. Only needed when
# `embodiment.enabled: true` in config.yaml. Stdlib-only itself.
afferent>=0.3.1  # optional (embodiment); brain runs fine without it
# Optional — the sleep-trained forward model (brain/forward_model.py). Only
# needed when training/using the learned world model; k-NN works without it.
numpy>=1.24  # optional (learned forward model — numpy fallback)
# Optional — robust GPU-trained forward model on Apple silicon. When present,
# the forward-model trainer uses MLX instead of the numpy MLP. Mac-only.
mlx>=0.18  # optional (forward model, Apple silicon)
# Optional — face identity (brain/face.py). Local ArcFace embeddings to
# recognize individuals + attribute app usage per person. Only needed when
# `face.enabled: true`; the whole feature no-ops cleanly without these.
insightface>=0.7  # optional (face identity); pulls onnxruntime + opencv
onnxruntime>=1.16  # optional (face identity backend)
# Optional — voice input (brain/voice.py). Local wake-word speech recognition
# ("hey brain"). Only needed when `voice.enabled: true`; no-ops without it.
# mlx-whisper is Apple-silicon native; faster-whisper is the cross-platform alt.
mlx-whisper>=0.4  # optional (voice input, Apple silicon)

# The brain reads OpenRouter credentials from ~/specter/.env by default.
# Override the location here if needed, or copy the keys into this file.
#
# SPECTER_ENV=~/specter/.env       # where to load credentials from
# OPENROUTER_API_KEY=sk-or-...     # only needed if not using ~/specter/.env
